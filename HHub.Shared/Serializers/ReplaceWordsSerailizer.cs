namespace HHub.Shared.Serializers;

internal sealed class ReplaceWordsSerailizer
{
    private bool _initiate = false;
    private Dictionary<string, string> _worldsToReplace = new();

    public void Initialize()
    {
        if (_initiate)
            return;

        
    }
}
